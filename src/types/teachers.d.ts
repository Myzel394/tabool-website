export interface TeacherApprox {
    shortName: string;
    lastName: string;
    id: string;
}

export interface TeacherDetail {
    firstName: string;
    lastName: string;
    shortName: string;
    email: string;
    id: string;
}
